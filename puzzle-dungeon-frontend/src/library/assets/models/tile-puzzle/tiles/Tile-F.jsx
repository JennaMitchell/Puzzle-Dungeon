/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.2.18 public/models/Tile-F.glb -o src/Tile-F.jsx -r public 
*/

import { useGLTF } from "@react-three/drei";
import { degreesToRadians } from "../../../../hooks/custom-project-hooks/degrees_to_radians";
import { Outlines } from "@react-three/drei";
import { startingTileData } from "../data/title-puzzle-data";
import { useSpring, animated } from "@react-spring/three";

export function TileF({
  positionX,
  positionZ,
  modelClickHandler,
  modelRef,
  textRef,
  elementClicked,
  moveTriggered,
  previousPositionX,
  previousPositionZ,
}) {
  const { nodes, materials } = useGLTF("/models/Tile-F.glb");

  const { x, z } = useSpring({
    from: {
      x: previousPositionX,
      z: previousPositionZ,
    },

    to: [
      {
        x: moveTriggered ? positionX : previousPositionX,
        z: moveTriggered ? positionZ : previousPositionZ,
      },
    ],

    config: {
      mass: 1,
      friction: 30,
    },
    onResolve: () => {},

    immediate: false,
  });
  return (
    <animated.group
      dispose={null}
      position-x={x}
      position-z={z}
      rotation-y={degreesToRadians(startingTileData.tileF.yRotationInDegrees)}
      onClick={modelClickHandler}
      scale={[1.35, 1, 1.35]}
    >
      <mesh
        geometry={nodes["tile-puzzle-F_v1_1"].geometry}
        material={materials["Mesh.001"]}
        ref={modelRef}
      >
        {elementClicked && <Outlines thickness={0.45} color="white" />}
      </mesh>
      <mesh
        geometry={nodes["tile-puzzle-F_v1_2"].geometry}
        material={materials["Text.001"]}
        ref={textRef}
      />
    </animated.group>
  );
}

useGLTF.preload("/models/Tile-F.glb");
